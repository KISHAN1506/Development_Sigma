// Q1 -> dice roll
for (let i = 0; i < 10; i++) {
    let random = Math.floor(Math.random() * 6) + 1;
    console.log(random);
}

// Q2:

let car = {
    name: "BMW",
    model: "M5",
    color: "Black"
};

console.log(car.name);

let person = {
    name: "Kishan",
    age: 19,
    city: "Surat"
};

person.city = "New York";
person.country = "US"

console.log(person);

