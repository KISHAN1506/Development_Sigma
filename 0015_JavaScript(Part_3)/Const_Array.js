const arr = [1, 2, 3];

// IN const in array , push pop everthing can be done
// Just the array cannot be replaced by new array

arr.push(4);
console.log(arr);
arr.pop();
console.log(arr);


// just this is not allowed , ie space address / reference cannot be changed
// arr = [1, 2, 3]