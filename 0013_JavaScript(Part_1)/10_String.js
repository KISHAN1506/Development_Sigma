let name = "Kishan";
let name2 = 'Kishan';

console.log(typeof(name));
console.log(typeof(name2));


let emp = "";
console.log(typeof(emp));

// In string double quote can contain single quote and vice versa 
// but can't have same type of quote inside