// These commands are very sensitive --> as files get permanently 
// deleted from everywhere, no way to recover 

// in a director --> rm app.js 

// rm command is to delete files not the whole Folders/Directory

// to remove Directory --> rmdir (removes only empty folder)

// rm -rf :: removes any folder , does not matter if 
// r--> recursive delete , f--> forced delete (Recursive Force) (remove with force)