// manual command

// if we want to get details of any of the command write "man ls" or so

// q to get out of manual

// Eg: ls -l : long information 
// ls -a : to show all the files be it hidden or normal

// ls -la : to combine both the flags 